Я создал все необходимые классы для работы почтой 
тебе всего лишь нужо сделать у себя команду composer install

потом обезателно посмотри как работает сам библиотека я в дискорде написал репозиторию этого библиотеки

#как правильно использовать

тебе не нужно уже настраивать библиотеку я его уже подготовил
тебе нужно лишь чуточку исправить config.php

думаю там ты поймешь что именно по примеру моему

дальше когда создашь класс который будет работать с почтой то обезательно нужно создать обект класса Mailer

$mailer = new Mailer();

после этого вызвать функциию которая настривает библиотеку.
$mailer->getMailConfig();
 а дальше просто из объекта $mailer вызываешь стандартные методы для отправки почты 
 $mailer->mail->функциию библиотки();
 принцыпи все удачи.
 обезателно выполняй в теле 
 try{
          $mailer->mail->функциию библиотки();
 }catch (Exception $e) {}